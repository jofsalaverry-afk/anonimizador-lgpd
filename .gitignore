node_modules
.env
.env.local
backend/node_modules
backend/.env
frontend/node_modules
frontend/.env

# PDFs de teste podem conter dados pessoais reais - nao commitar
backend/*.pdf

# Lab isolado de avaliacao Azure DI — fixtures, responses e scripts
# nao entram no repo (podem ter PDFs com PII real, keys vazadas em
# response cruda, etc.)
backend/_lab_azure/

.claude/
